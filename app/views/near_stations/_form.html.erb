<%= form_for(near_station) do |f| %>
  <% if near_station.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(near_station.errors.count, "error") %> prohibited this near_station from being saved:</h2>

      <ul>
      <% near_station.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :line_name %>
    <%= f.text_field :line_name %>
  </div>

  <div class="field">
    <%= f.label :take_minutes %>
    <%= f.number_field :take_minutes %>
  </div>

  <div class="field">
    <%= f.label :real_estate_id %>
    <%= f.number_field :real_estate_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
